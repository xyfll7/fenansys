{
  "name": "haluoxyf",
  "version": "1.0.4",
  "description": "ts-koa",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "xyf",
  "license": "MIT",
  "scripts": {
    "start": "yarn serve",
    "serve": "nodemon dist/server.js",
    "build": "yarn tslint && yarn build-ts",
    "build-ts": "tsc",
    "watch": "yarn tslint && yarn watch-ts",
    "watch-ts": "tsc -w",
    "tslint": "tslint -c tslint.json -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  }
}
